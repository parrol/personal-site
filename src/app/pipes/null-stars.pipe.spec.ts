import { NullStarsPipe } from './null-stars.pipe';

describe('NullStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new NullStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
